header.header 
    nav.nav.nav--tours
        a.nav__el.nav__home-btn(href='/home')
            svg.icon-home
                use(xlink:href='/img/icons.svg#icon-home')
        a.nav__el(href='/') All tours 
        from.nav__search 
            button.nav__search-btn
                svg
                    use(xlink:href='/img/icons.svg#icon-search')
            input#search-input.nav__search-input(type='text',name='quey', placeholder=' Tour search', value='')
    .header__logo
        img(src='/img/logo-white.png' alt='Natours logo')
    nav.nav.nav--user
        if user
            a.nav__el.nav__el--logout Log Out 
                svg.icon-small
                    use(xlink:href='/img/icons.svg#icon-log-out')
            a.nav__el(href='/me')
                img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
                span=user.name.split(' ')[0]
        else
            svg.icon-small
                use(xlink:href='/img/icons.svg#icon-log-in')
            a.nav__el(href='/login') Log in
            a.nav__el.nav__el--cta(href='/singUp') Sign Up